<!-- BEGIN:blog29-studio -->
## 공동 조사·토론 모드 (선택 적용)

- 사용자가 공동 조사/토론 후 글·발표자료 제작을 요청하거나, 현재 주제의 `brief.md`에 `mode: studio`가 있으면 `docs/blog29/STUDIO.md`와 해당 작업 메모를 읽는다. 단순 편집·명시적 즉시 집필 요청에는 이 모드를 억지 적용하지 않는다.
- Studio의 기본 도착점은 `DISCUSS`다. 일반 집필 절차의 자동 초안 작성은 유예한다. 조사→토론→추가 조사 루프는 가능하지만 사용자에게 판단 질문을 제시한 뒤에는 답변을 기다린다. 스스로 사용자의 동의나 다음 메시지를 만들지 않는다.
- 사용자 수집 자료와 에이전트 추가 조사 자료의 유입 경로를 구분하되 같은 증거 기준으로 평가한다. 의견 채택과 사실 검증은 서로 다른 상태다.
- `brief.md`, `sources.md`를 재사용하고, `discussion.md`에 결정·유보·수정 이유를 남긴다. 합의한 내용은 `story.vN.md`로 고정한다. 이는 편집 기준본이며 객관적 진실의 확정을 뜻하지 않는다.
- “이 구성으로 써줘/발표자료로 만들어줘”처럼 구성 채택과 작성을 명시한 요청은 그 범위의 승인이다. 같은 승인을 다시 묻지 않는다. 단순 호응·질문·자료 추가는 작성/게시 승인으로 취급하지 않는다.
- Studio 집필에서는 기준본의 논지·근거·한계를 보존한다. 블로그와 슬라이드는 같은 기준본에서 각각 구성한다. 원고 생성 권한과 사이트 반영·commit·push 권한은 구분한다.
- 기본 작업 경로는 안전성을 확인한 `.blog29-work/<slug>/`다. Git 추적·콘텐츠 로더·업로드 포함 여부를 확인할 수 없으면 원자료를 저장하지 않고 저장소 밖의 승인된 경로를 사용한다. 그 경우 Studio에서는 해당 경로를 기존 집필 스킬의 하드코딩 경로보다 우선하고 `brief.md`에 기록한다.
- 웹 검색/원문 읽기, 파일 읽기, 렌더링은 실제 실행 가능할 때만 수행했다고 보고한다. 필요한 도구가 없으면 가능한 범위만 진행한다. 이 모드는 자동 실행 엔진이나 권한 강제 장치가 아니다.
- 기존 Next.js 규칙, 출처·보안·Git·Vercel 규칙은 그대로 유지한다. 설정/모델/의존성을 이 모드 활성화만으로 변경하지 않는다.
<!-- END:blog29-studio -->
